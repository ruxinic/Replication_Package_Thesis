# Preliminary Benchmarks

This folder contains the non-complex benchmarks we constructed to test out most guidelines before the start of the experiment. Their purpose was to observe the optimizations' behavior and become familiar with their implementations.

The executions were performed on the local Windows laptop, with a separate script, not the one used for the actual experiment. EnergiBridge was installed and used locally.

All resulting CSV files are in this folder.
